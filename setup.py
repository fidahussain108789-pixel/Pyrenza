from setuptools import setup, find_packages

setup(
    name="pyrenza",
    version="0.1.0",
    description="Variables that remember everything.",
    packages=find_packages(),
    python_requires=">=3.10",
)
